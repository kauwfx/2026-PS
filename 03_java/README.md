#

Conceito pretendido: B

Justificativa:

Exemplo 1–2 (arrays + métodos): `calcularMedia(double[] notas)` e `contarAprovados(double[] notas)`

Exemplos 3 (ArrayList): `adicionarProduto(ArrayList<String> lista, String nome)` e `listarProdutos(ArrayList<String> lista)`

Exemlos 4 (sobrecarga): `maiorValor(int[] valores)` e `maiorValor(int a, int b)`

Exemplos 5 (integração): `exibirBoletim(double[] notas)` reutiliza `calcularMedia()` e `contarAprovados()`

Autoavaliação: esta seção do README
